import { Component } from '@angular/core';
import { ViewEncapsulation } from '@angular/core';

@Component({
    selector: 'my-restore',
    template: require('./restore.component.html'),
    encapsulation: ViewEncapsulation.None
})

export class MarkupRestoreComponent {
}
